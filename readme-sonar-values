ingress:
  enabled: true
  # 1. Khai báo dùng chung hạ tầng Ingress NGINX hiện tại
  ingressClassName: nginx 
  
  # 2. Sử dụng mẹo sslip.io với IP tổng của bạn để không cần sửa file hosts
  hosts:
    - name: sonar.34.81.14.40.sslip.io
      path: /
      
  # 3. Annotation chuẩn để NGINX Controller nhận diện luật định tuyến
  annotations:
    kubernetes.io/ingress.class: nginx

# 🔐 BỔ SUNG THÊM ĐOẠN NÀY ĐỂ FIX LỖI VALIDATION
monitoringPasscode: "123456"  # <-- Bạn điền chuỗi mật khẩu bí mật bất kỳ vào đây

community:
  enabled: true